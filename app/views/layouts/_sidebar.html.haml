#main_sidebar
  #sidebar_top_divider
    = image_pack_tag("divider.png", :class => "divider")
  #sidebar_content
    = image_pack_tag("#{@character.avatar_image_path}", :class => "avatar_icon_large", :id => "sidebar_avatar")
    #right_hand_wrapper
      %h4
        Welcome, #{@character.name}
    #main_stats_display
      = render 'characters/characterstats'
  #sidebar_bottom_divider
    = image_pack_tag("divider.png", :class => "bottom_divider")

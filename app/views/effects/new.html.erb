<h1>Add a New Effect</h1>

<% unless @story_id.nil? %>
  <p>Existing Effects for Branch: <b><%= @story.title %></b></p>
    <% if @effects.size > 0 %>
      <%= render 'effecttable' %>
    <% else %>
      <p><i>There are no effects for this branch yet.</i></p>
    <% end %>
<% end %>
<br>
<p><b>New Effect:</b></p>
<%= render 'effectform' %>

<p><%= link_to "Return to Branches Index", branches_path %></p>
<p><%= link_to "Return to Stories Index", stories_path %></p>